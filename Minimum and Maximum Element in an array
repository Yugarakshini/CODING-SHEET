class Solution {
    static Pair getMinMax(long a[], long n)
    {
        Arrays.sort(a);

        long mn = a[0];
        long mx = a[(int)n - 1];

        Pair ans = new Pair(mn, mx);

        return ans;
    }
}
