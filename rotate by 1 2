class Solution {
    public void rotate(int[] arr) {
        int i=0,j=arr.length-1;
        while(i!=j){
            swap(arr[i],arr[j]);
            i++;
        }
    }
    
    public void swap(int i,int j){
        int temp = arr[i];
        arr[i] = arr[j];
        arr[j] = temp;
    }
}
